// the brand palette, shared by every widget and by the markdown pages that color headings, dividers, and highlighted text. the same six names are declared for the build in generator.js and as custom properties in style.css, so a change here has to be made in all three.
let colors = ['pink', 'green', 'blue', 'yellow', 'purple', 'red'];
window.colors = colors;
